session*
data/
__pycache__/
*.pyc
*.pyo

# Build artifacts
build/
*.egg-info/
dist/

# Generated outputs
Overflow_Graph/

# User config (default template is tracked as config.default.json)
config.json
config_path.txt

# Node modules
node_modules/

# Virtual environments
venv/
venv*/
.venv/
env/
.env/
*.odt
*.docx
config_*.json
.idea/
*DS_Store

# Legacy hand-maintained standalone — kept on disk for reference
# during the migration to the auto-generated single-file build
# (`frontend/dist-standalone/standalone.html` via `npm run build:standalone`).
# Do NOT track further changes. Canonical UI is now React + Vite.
standalone_interface_legacy.html
